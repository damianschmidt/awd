version: "3.8"
services:
  backend:
    container_name: "awd_backend"
    build:
      context: ../backend
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - "../backend:/backend"
